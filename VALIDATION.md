# 验证记录

## 1.0.8 紧凑间距与简明说明

- 31 项已有自动化测试及两个受支持客户端版本的模拟界面回归通过。
- 核对卡片内容间距为 6px，按内容自然排布，不再设置固定最小高度；名称 16px、数值 20px 保持不变。
- 浅色/深色及窄窗口检查通过；余额、Token 刷新、任务切换与重新加载行为通过回归。
- 首页包含安装包直达链接、安装/更新步骤和常见问题；技术资料保留在 `docs/REFERENCE.md`。
- 本轮仅使用模拟 API 数据与临时测试配置。

## 1.0.7 统计名称与明细精简

- 31 项已有自动化测试及两个受支持客户端版本的模拟界面回归通过。
- 检查统计卡片名称为 16px、分区标题为 17px，卡片内无说明小字，“其中推理”已改为“推理”。
- 模拟套餐仍显示正确的套餐余额及每日明细，不再显示每月剩余行；余额计算使用的采集逻辑没有修改。
- 查看浅色/深色、Token 卡片顶部和底部截图；窄窗口、卡片等高、刷新、任务切换、重新加载与官方登录停用检查通过。
- 本轮仅使用模拟 API 数据与临时测试配置，未查询真实账号或重启客户端。

## 1.0.6 排版调整

- 31 项已有自动化测试及两个受支持客户端版本的模拟界面回归通过。
- 使用模拟钱包和套餐数据检查浅色/深色截图：放大的主标题、收起按钮与分区标题、金额垂直对齐，以及更大的 Token 标签和数字。
- 检查余额更新时间位于余额区，页脚仅显示版本号；标题小字、数据来源、无数据的缓存写入说明和指定计费说明均已移除。
- 验证 Token 卡片等高、数字左对齐，720 / 480 / 360 CSS 像素窗口下没有面板横向溢出；保留刷新、任务切换、旧值保护和重新加载行为。
- 本轮只使用模拟 API 数据和临时测试配置，未查询真实账号、切换供应商或重启客户端。

## 1.0.5 界面整理

- 31 项已有自动化测试通过，包含余额与套餐解析、Token 读取、安装、更新和回滚。
- 两个受支持客户端版本的模拟界面回归通过：Token 刷新、同任务旧值保护、任务切换和脚本重新注入。
- 用模拟 API 数据检查金色套餐徽章的浅色/深色显示、长名称和到期状态，以及钱包或查询失败时移除套餐徽章。
- 检查余额区只保留刷新按钮，旧版存储的独立查询账户被忽略，请求跟随当前供应商。
- 验证 Token 标题与刷新按钮垂直居中、总量卡片间距、统计卡片等高，以及 720 / 480 CSS 像素窗口下的面板边界和横向溢出。
- 所有本轮界面验证均使用模拟数据，未查询真实账号、切换实际供应商或重启用户客户端；发布后的客户窗口仍需正常重新加载新版脚本。

## 1.0.4 API Token 修复

- 31 项自动化测试通过，新增大日志、超长非统计行、分块边界、未写完的末行、任务身份校验、只读文件权限以及 Windows 扩展路径处理。
- 在真实 Windows 只读沙箱中验证 Token 查询程序：带扩展前缀的路径规范化后，成功读取超过 16 MiB 的任务日志，只返回筛选后的统计数据。未修改日志权限或切换供应商。
- 两个支持的客户端版本均通过模拟 API 界面测试：Token 显示、独立刷新、读取失败保留并标记同任务旧值、切换任务不串数、快速路由切换、脚本重新注入、余额及套餐显示。
- 官方直接登录使用模拟数据验证：不请求官方账号或额度接口，不读取该模式的任务 Token，默认隐藏 API 工具栏。
- 尚未在反馈问题的客户电脑上复验；截图中的旧版通用提示不足以断言该电脑唯一的失败环节，新版提供具体错误代码。

## 1.0.3 更新网络兼容

- 25 项自动化测试通过，新增代理环境变量优先级、Windows 静态单代理/分协议代理、禁用代理和不支持协议的处理测试。
- 在已启用 Windows 系统代理的开发机复现 Node 直连 GitHub 下载超时；读取现有系统代理后，匿名获取公开 Release 元数据和 ZIP 成功，资产 SHA-256 核验通过。未修改系统代理或读取 GitHub 登录凭据。
- 继续保留 1.0.2 的真实 Windows 沙箱复现与修复结论；客户电脑上的实际余额仍待安装后复验。

## 1.0.2 修复验证

- 24 项自动化测试通过，包含原有数据、安装与界面逻辑测试，以及新增的沙箱 ACL 和在线更新回归。
- 在临时目录安装查询程序，将测试副本 `node.exe` 的 ACL 限制为仅当前用户可访问。真实 Codex App Server 使用 Windows 只读沙箱启动该程序时，复现 `CreateProcessAsUserW failed: 5`；执行新 `repair` 后，同一通道返回 `self-test-ok`、退出码 0。只操作临时测试文件，未修改当前用户的 Codex 沙箱配置或重启客户端。
- 权限测试在 PowerShell `Restricted` 环境验证：只增加已有沙箱组读取/执行权限，无写权限、无子级继承；凭据文件权限不变，已有拒绝规则不变；重复修复幂等。
- 真实 CMD 测试覆盖中文、空格、`&` 路径及禁止 PowerShell 脚本的进程策略；移走下载目录后，开始菜单修复入口与固定卸载入口仍能工作。
- 在线更新测试使用模拟 GitHub 响应和真实安装器完成旧版升级、固定入口安装及回滚；验证损坏下载、HTTP 失败、安装前失败、错误路径、重复文件、不可信重定向及用户修改文件受到保护。相同版本和旧版不会被下载安装。
- 两个支持客户端版本的模拟浏览器测试通过：钱包/套餐显示、`collector_runtime_access` 修复提示、出错后清空旧余额，错误不包含原始敏感文本，版本显示为 1.0.2。
- 客户新截图中的日志及文件权限检查，与此次真实沙箱复现吻合；仍需要客户安装 1.0.2 后确认实际余额显示。测试通过不等同于已在客户电脑验收。

## 1.0.1 修复验证

- 19 项自动化测试通过，新增覆盖真实 CMD 启动、PowerShell `Restricted` 进程策略、中文/空格/`&` 路径、重复安装、诊断及卸载。
- 固定 Node 运行时通过启动自检；显式配置文件路径在不同用户主目录环境下仍正确；验证 Node 拒绝文件写入和子进程启动。
- 在独立的真实 Codex App Server 上，通过 `command/exec` 成功运行新安装的查询程序；使用临时模拟配置，未切换真实供应商或重启用户客户端。
- 两个客户端版本的模拟浏览器测试均通过：钱包/套餐显示、沙箱错误提示、失败后清除旧余额、错误中不回显原始敏感文本。
- 第三张用户截图只提供旧版通用错误，没有实际进程或 RPC 错误。因此尚不能断言那台电脑的唯一根因；本版修复运行时路径、工作目录、强制沙箱等可移植性缺陷，并提供进一步定位所需的错误代码。
- 尚未在反馈问题的那台电脑复验；Node 自动下载路径依赖 nodejs.org 可达，哈希按官方 24.19.0 清单固定。

## 已完成

- 余额查询使用真实 Sub2API 服务商验证，并通过独立 Codex App Server 的 `command/exec` 通道验证。真实账户、域名和账单数字未包含在仓库中。
- 12 项数据测试覆盖钱包、套餐周期、到期、无限额度、零余额、额度单位、重定向、鉴权失败、精确账户选择和本机任务匹配。
- 安装器测试覆盖含空格路径、重复安装、未知脚本保护、已验证旧版迁移、回滚及卸载备份。
- 开发环境中的两个客户端适配版本通过模拟浏览器测试：首页与任务挂载、展开收起、任务切换、重新注入、清理、钱包与套餐显示、失败时清除旧账户数据。
- 0.1.1 已由用户截图确认在真实 Codex++ 窗口挂载成功。

## 尚未作为真实整体验收的项目

- 1.0.0 完整真实客户端窗口显示，仍待正常重新加载后确认。验证时按用户要求没有重启当前客户端。
- 当前用于真实接口验证的密钥为钱包计费；套餐解析使用依据接口协议构造的测试数据，尚未用真实套餐密钥单独验收。
- New API 适配器有响应格式测试，未对真实 New API 账户作端到端验证。

测试通过与真实客户端验收是不同状态。发布版本号不代表支持所有服务商、所有客户端版本或缺失的接口字段。
