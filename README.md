# Codex++ Usage Toolbar

Codex++ API 顶部用量栏：查看 API 钱包余额或当前密钥套餐，以及本机 API 任务 Token、缓存和时钟。

**版本：1.0.5。** 独立用户脚本项目，依赖 Codex++ 的脚本加载功能，不是 Codex++ 客户端分发包，也不是 OpenAI 官方扩展。

## 能显示什么

- **钱包计费**：当前查询密钥对应的钱包剩余余额。
- **套餐计费**：接口返回的套餐名称、剩余额度、日／周／月用量和有效期。不会凭供应商名称判断是否属于套餐。
- **密钥限额**：接口只提供密钥限额时显示密钥额度，不冒充钱包或套餐。
- **当前本机 API 任务**：累计 Token、输入、缓存输入、输出、推理、缓存命中率和上下文剩余估计。
- **刷新**：本地时钟、手动刷新、账户与余额约 60 秒刷新、任务约 5 秒检查；隐藏窗口暂停轮询。

本版专用于 API。不会请求 `account/read` 或 `account/rateLimits/read`，不会读取官方账号额度。默认跟随供应商时，官方直接登录下隐藏 API 工具栏，暂停任务 Token 查询；官方原生“使用量”窗口由 Codex 自身管理。

余额查询统一跟随 Codex++ 当前供应商，点击“立即刷新”获取最新数据。升级后不再使用旧版保存的独立查询账户；**不会切换实际供应商或重启对话**。

## 兼容范围

| 项目 | 范围 |
| --- | --- |
| 系统 | Windows |
| Node.js | 双击安装时自动检测兼容性；不兼容时下载官方 24.19.0 便携运行时 |
| Codex++ | 1.2.56 用户脚本机制 |
| Codex 内置版本 | 26.831.20005、26.901.20858 |
| API 接口 | Sub2API `/v1/usage`、New API `/api/usage/token` |

客户端升级后，如未识别到受支持的版本，脚本会停止并报告兼容性问题；不会宽松匹配客户端内部代码。

## 安装

1. 已安装上表所列版本的 Codex++。
2. 从 [最新发布页](https://github.com/WKing-1217/codexplusplus-usage-toolbar/releases/latest) 下载 `codexplusplus-usage-toolbar-v1.0.5.zip`，**先完整解压**。
3. **双击 `install.cmd`**，等窗口显示“安装成功，查询程序启动自检通过”。无需手输命令，无需管理员权限。
4. 在 Codex++ 管理工具中重新加载用户脚本。若使用重启按钮，请先保存当前工作；安装器不会自动重启或结束对话。

原先出现“在此系统上禁止运行脚本”的用户，请使用 `install.cmd`，无需修改 PowerShell 执行策略。窗口会保留结果，不会失败后直接闪退。

安装器优先使用本机兼容的 Node.js（24.14+、25.4+ 或更高主版本），并把运行时复制到插件自己的固定目录，避免切换 Node 版本或删除下载文件夹后查询失效。缺少兼容 Node 时，自动从 **nodejs.org** 下载 Windows x64/ARM64 的 Node 24.19.0，验证固定 SHA-256 后才执行；首次下载需要联网。不会修改系统 PATH 或覆盖系统 Node。网络下载失败时可重试，或从 [Node.js 官网](https://nodejs.org/) 安装 24.19.0 后再次双击。

仓库已包含 `dist`，无需 npm 下载依赖。管理工具的“脚本市场 → 本地脚本”可查看 `codex-usage-toolbar.js`。升级旧版同样双击 `install.cmd`，自动备份旧版；如果只是重复安装，则保留原回滚点。

### 以后怎样更新（不用再删旧文件、重新下载）

第一次从 1.0.0 / 1.0.1 升级到本版，仍需下载本版并双击一次 `install.cmd`。之后：

- **开始菜单 → Codex++ Usage Toolbar → Codex++ 用量栏更新**，点击一次即可。
- 也可以双击下载包的 `update.cmd`，或固定目录 `%APPDATA%\Codex++\usage-toolbar\update.cmd`。
- 更新器检查本仓库最新正式 Release，下载专用更新包，核对 GitHub 提供的 SHA-256 和每个文件的校验值后安装。不会自动降级，不需要 Git、GitHub 登录或手动下载 ZIP。
- 更新时自动保留旧版回滚点、供应商配置和会话。下载失败或校验不符时保留当前版本；可稍后重试。
- 安装后的管理程序保存在插件目录，原来的下载文件夹可以移走；开始菜单入口仍有效。更新后按提示在 Codex++ 中重新加载脚本，不会自动重启或打断当前对话。

更新器支持已有 `HTTP_PROXY` / `HTTPS_PROXY` 环境变量；未设置时读取 Windows 已启用的静态 HTTP/HTTPS 系统代理，不更改系统代理、不关闭 TLS 校验。仅 PAC / SOCKS 代理环境暂不自动适配，可通过代理软件的系统 HTTP/HTTPS 模式访问 GitHub。下载受阻时可稍后重试；从 1.0.2 更新遇到代理超时的用户，可下载本版 ZIP 双击安装一次。

“修复”和“诊断”也有开始菜单入口。`repair.cmd` 只修复已安装查询程序的文件访问权限，不联网查询余额。

### 显示“余额查询失败”怎么办

1. 先点击工具栏“立即刷新”，查看完整错误代码。
2. **双击 `diagnose.cmd`**，窗口会说明程序、运行时和配置检查结果；可分享生成的 `diagnostics.json`。报告只含状态、版本和错误代码，不包含密钥、账号名称、服务商地址或余额。
3. 文件缺失（`collector_missing`）、运行时不兼容（`collector_runtime`）或安装不完整时，重新双击 `install.cmd`。文件被手动修改时会停止，避免覆盖其他人的修改。
4. `CreateProcessAsUserW failed: 5` / `collector_runtime_access`：**双击 `repair.cmd` 或开始菜单的“Codex++ 用量栏修复”**，然后点“立即刷新”。1.0.2 的安装和更新也会自动执行这项修复。
5. 沙箱错误（`collector_sandbox`）或诊断中的 `group-missing`：先在 Codex 中完成沙箱初始化，再运行修复。插件不创建沙箱账号、不改变对话的权限模式。当前对话设置成完全访问，不能代替后台查询进程的文件权限。

1.0.1 的安装自检只验证了当前用户可以启动查询程序，遗漏了独立沙箱账号对 `runtime\node.exe` 的读取/执行权限，因此会出现 Token 已显示、余额程序仍启动失败。1.0.2 会给已有的本机 `CodexSandboxUsers` 组补齐**插件运行时、查询脚本及其插件内父目录**的读取/执行权限，并复查结果。规则只作用于这些文件/目录，不授予写入、不放宽已有拒绝规则，也不修改密钥配置文件的权限。

`localProcess: ok` 说明 Windows 可以运行查询程序；`codexBridge: not-tested` 说明双击诊断无法代替 Codex 窗口内的命令通道测试。`sandboxAccess` 显示文件 ACL 检查结果：`ready` 表示所需组规则已配置，不能代替真实进程执行；`missing-read-execute` 表示需修复，`explicit-deny` / `acl-error` 表示权限配置受阻。若本机诊断成功而工具栏仍失败，请同时提供工具栏错误代码。`configuration` 是供应商配置读取问题，`network` 是服务商连接问题，两者与“程序无法启动”分开显示。

### Token 不显示怎么办

1. 确认重新加载脚本后底部显示 **1.0.5**。仍显示旧版本号，说明窗口还在运行旧脚本。
2. 打开具体的本机 API 任务，在详情中点击 **刷新 Token**。该按钮独立于余额刷新。
3. 本版对 Codex 返回的 Windows 扩展路径进行规范化，查询程序的只读授权和实际读取使用同一条路径；从日志末尾分块查找最新 Token，日志总大小超过 16 MiB 也可以显示。
4. 暂时读取失败会保留**同一个任务**上次成功的数据，显示更新时间和错误提示；切换任务会清空旧任务数据。权限不足、文件移动、身份不匹配和还没有统计记录分别提示。

Token 查询仅允许读取已由 `thread/read` 精确返回的当前任务文件，再核对日志首条 `session_meta` 中的任务 ID。不会遍历其他任务，不使用“最近修改的文件”猜测当前对话，也不把聊天正文传回界面。单次最多向前扫描最近 64 MiB，超出范围未找到统计时明确提示，不显示虚假的零。

### 已有本地旧版

如果同名脚本由本项目早期本地安装器生成，可传入原安装收据；只有当前文件与收据的 SHA-256 一致才迁移：

```powershell
node .\scripts\manage.mjs install --previous-receipt '原安装目录\installation.json'
```

其他同名脚本会被保留，安装器会明确报错。

## 使用

点击顶部摘要展开详情。

- 使用 API 钱包时，摘要显示钱包余额。
- 使用套餐时，摘要和详情以金色金属质感徽章显示接口返回的套餐名称，并显示剩余额度、周期用量与有效期。不会根据服务商名称虚构套餐等级。
- API 余额和当前任务 Token 的刷新按钮分别位于各自标题右侧，统计卡片统一对齐。
- 查询对象切换时先清空旧数据；查询失败显示具体状态，不使用别的账户数据补位。
- 零余额、无限额度、到期、缺少字段分别处理。接口没有给出的数据保持未知。

New API 的令牌用量接口返回内部额度单位，本项目不按未经确认的固定倍率将它换算成美元。服务商扣费属于此密钥的账单统计，与当前任务 Token 分开显示。

## 回滚、卸载、状态

双击 `rollback.cmd` 回滚，双击 `uninstall.cmd` 卸载。原有 PowerShell 入口继续保留。命令行查询状态：

```powershell
node .\scripts\manage.mjs status
```

回滚与卸载先校验现有脚本，保留备份，不碰账号、供应商配置或其他脚本。磁盘变化在脚本下次加载时生效。卸载后保留的查询程序不会自行运行。

运行数据、收据和备份存放于 `%APPDATA%\Codex++\usage-toolbar`；本机生成的脚本在 `%APPDATA%\Codex++\user_scripts`。这些文件不应提交到仓库。

## 凭据与通信

余额查询通过 Codex 的 `command/exec` 启动一个短时 Node 进程，沿用本机现有的 Codex 沙箱配置，并指定固定工作目录和当前用户的配置文件路径。余额查询模式仅允许读取查询程序和供应商配置；Token 模式仅允许读取查询程序和该任务日志，不读取供应商配置、不发起网络请求。两种模式均不授予文件写入、子进程或原生扩展加载权限；余额网络查询仅访问配置中服务商的已知接口。Node 权限模型是应用层限制，不替代操作系统沙箱。返回经过筛选的额度数据后进程退出。

- 没有常驻服务、额外监听端口、计划任务或遥测上传；在线更新只在用户点击更新入口时访问本仓库的 GitHub Release。
- 密钥不写入用户脚本、命令参数、界面或日志；服务商错误正文不回传界面。
- 不携带密钥跟随 HTTP 重定向，不发送聊天正文。
- 不修改 WindowsApps、官方客户端归档、系统级权限策略或 Codex 启动快捷方式；只为本插件运行文件配置沙箱组读取/执行权限，创建本插件的维护入口。
- 不执行购买、充值或消耗重置次数的操作。

更新代理使用 [Node.js 内置代理支持](https://nodejs.org/api/http.html#httpsetglobalproxyfromenvproxyenv)。首次准备运行时的下载地址和哈希来自 [Node.js 24.19.0 官方校验清单](https://nodejs.org/download/release/v24.19.0/SHASUMS256.txt)。

## 开发与验证

```powershell
npm run build
npm test
npm run check
```

源码位于 `src`；构建生成不含本机路径的 `dist` 模板和校验清单；安装时才写入本机路径。安装器测试仅操作临时目录，不查询真实服务商。

验证情况与实际限制见 [VALIDATION.md](VALIDATION.md)。支持精确匹配的本机 API 任务；远程任务不借用本机其他任务的数据，历史用量与当前任务的实时通知结合显示。

## 协议参考

- [Codex++ 用户脚本机制](https://github.com/BigPizzaV3/CodexPlusPlus/tree/v1.2.56)
- [Sub2API 用量接口实现](https://github.com/Wei-Shaw/sub2api/blob/main/backend/internal/handler/gateway_handler.go)
- [New API 令牌用量接口](https://github.com/QuantumNous/new-api-docs/blob/main/docs/api/token-usage.md)

仓库只包含本项目的用户脚本、安装器和测试，不包含 Codex 客户端二进制、解包源码、用户密钥或真实账单截图。
