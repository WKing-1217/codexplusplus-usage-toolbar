{
  "name": "codexplusplus-usage-toolbar",
  "version": "1.0.0",
  "private": true,
  "description": "Codex++ 顶部额度、API 钱包与套餐用量栏",
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs tests/*.test.mjs",
    "check": "node --check src/toolbar.js && node --check src/balance.cjs"
  }
}
