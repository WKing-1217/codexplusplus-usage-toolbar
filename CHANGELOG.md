# 更新记录

## 1.0.7

- 放大输入、缓存读取、非缓存输入、输出、推理、缓存命中率和上下文剩余估计的卡片名称。
- “其中推理”改为“推理”，删除输入、推理和上下文卡片中的说明小字。
- 再次放大“API 余额”和“当前任务 Token”分区标题。
- 套餐详情移除“每月剩余”行；套餐余额计算及日/周周期明细保持原有逻辑。

## 1.0.6

- 删除标题区的小字，将“用量详情”放大为主标题；放大收起按钮和分区标题。
- 金额与整行信息垂直居中，带说明的扣费行不再将金额挤在顶部，Token 统计卡片保持左对齐。
- 删除面板中的服务商数据来源行。
- 余额更新时间移到余额区底部，页脚只保留版本号，不再显示轮询间隔。
- 放大 Token 卡片标签、数字、间距，统一卡片高度，保持名称与数值左对齐。
- 删除缺失缓存写入时的占位说明、非缓存输入计费说明和今日扣费下的小字。

## 1.0.5

- 套餐名称改用金色金属质感徽章，顶部摘要与详情保持一致；兼容深色主题、长名称和到期状态。
- 移除“API 用量 / 只读”行、余额账户选择框及其说明，余额标题右侧仅保留“立即刷新”。
- 查询统一跟随当前供应商，忽略旧版保存的独立查询账户，避免隐藏选择继续影响显示。
- Token 标题和刷新按钮同排，总量卡片留出间距，统计卡片统一高度和数字对齐。
- 保留 API 余额、套餐解析、任务读取、安装与一键更新；不调用官方账号用量接口。

## 1.0.4

- API 任务 Token 改由短时只读查询程序读取，兼容 Windows 扩展路径，避免整份日志跨界面传输。
- 从日志末尾分块查找最新 Token 记录；支持总大小超过 16 MiB 的日志，单次扫描最多 64 MiB。
- 增加独立“刷新 Token”按钮、更新时间、具体错误提示和同任务的上次成功数据保留；切换任务不会复用别的任务统计。
- 读取前同时核对任务 ID、日志路径和 `session_meta`，不遍历或猜测其他会话。
- 明确限定为 API 工具栏，移除官方账号额度的自动查询；当前使用官方直接登录时暂停任务 Token 查询。
- 保留余额、套餐、权限修复和一键更新功能。

## 1.0.3

- 在线更新优先使用已有代理环境变量，否则自动读取 Windows 静态 HTTP/HTTPS 系统代理，修复浏览器能打开 GitHub、更新器却直连超时的情况。
- 安装入口同时检测 Node 的内置代理兼容性；不兼容时自动准备已校验的 24.19.0 运行时。
- 不修改系统代理或 TLS 校验，下载仍仅允许 GitHub 发布地址及其官方资产主机。
- 完整包含 1.0.2 的余额沙箱权限修复、固定维护入口和更新校验。

## 1.0.2

- 修复 Windows 沙箱启动余额程序时的 `CreateProcessAsUserW failed: 5`：安装、更新和回滚时检查插件运行时及查询脚本，给已有沙箱组补齐读取/执行权限；不授予写入、不改变现有拒绝规则。
- 新增 `repair.cmd` 和具体权限错误提示；诊断同时检查当前用户运行结果与沙箱组文件 ACL。
- 新增 `update.cmd` 和开始菜单中的更新、修复、诊断入口。维护程序随安装保存在固定目录，移走下载文件夹后仍可使用。
- 点击更新即可从本仓库最新正式 Release 下载专用更新包，验证来源、SHA-256、文件清单和版本后安装，保留旧版本回滚点；不自动降级或重启对话。
- 通过真实 Codex Windows 沙箱复现同款权限拒绝，修复后同一查询自检成功；加入权限、在线更新、下载失败和回滚回归测试。

## 1.0.1

- 增加双击 `install.cmd`、`diagnose.cmd`、`rollback.cmd`、`uninstall.cmd`，不再要求用户手输 PowerShell 命令或修改执行策略。
- 缺少兼容 Node 时自动下载并校验官方 Windows 便携运行时；安装器固定运行时、查询目录和配置文件路径，并在替换脚本前验证查询程序可以启动。
- 查询沿用用户已配置的 Codex 沙箱，移除硬编码的沙箱模式；通过 Node 权限模型限制配置读取和文件写入。
- 启动、沙箱、文件缺失、超时和数据格式错误分别显示代码与处理办法；增加不包含密钥或账单的诊断报告。
- 保留 1.0.0 升级备份与回滚，不重启客户端、不切换供应商。

## 1.0.0

- 加入通过本机短时查询程序读取的 Sub2API 钱包、套餐和 New API 密钥限额。
- 按接口返回的计费类型显示钱包余额，或套餐名称、剩余额度与有效期。
- 加入独立余额账户选择、手动刷新和查询错误状态；默认跟随当前供应商。
- 保留当前本机任务 Token、缓存、上下文估计和时钟。
- 将本机专用安装包改为便携模板，安装时自动确定路径，加入校验、迁移、备份、回滚和卸载。

## 0.1.1

- 适配 Codex 26.901.20858，修复首页挂载和异步加载状态。

## 0.1.0

- 首个 Codex++ 用户脚本版本。
